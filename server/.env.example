# Server Configuration
PORT=3000

# Database
# Replace with your actual MongoDB connection string
# Format: mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/DATABASE_NAME?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://REPLACE_WITH_YOUR_USERNAME:REPLACE_WITH_YOUR_PASSWORD@your-cluster.mongodb.net/TaskWell?retryWrites=true&w=majority

# JWT Secret (use a secure random string in production)
# IMPORTANT: This must be the same across all environments (dev, staging, production)
# to ensure tokens generated in one environment work in another
# Generate a secure random string: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=REPLACE_WITH_SECURE_RANDOM_STRING_AT_LEAST_32_CHARACTERS_LONG

# Frontend URL (for CORS and email links)
# Development: http://localhost:5173
# Production: https://your-app.vercel.app
FRONTEND_URL=http://localhost:5173

# Email Configuration (Brevo)
# Get your API key from: https://app.brevo.com/settings/keys/api
BREVO_API_KEY=xkeysib-REPLACE_WITH_YOUR_ACTUAL_BREVO_API_KEY
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=TaskWell

# Arcjet Security
# Get your key from: https://app.arcjet.com
ARCJET_ENV=development
ARCJET_KEY=ajkey_REPLACE_WITH_YOUR_ARCJET_KEY
